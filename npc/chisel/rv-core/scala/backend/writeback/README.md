# 写回与寄存器文件

本目录保存提交阶段可见的寄存器状态：

- `RegisterFile.scala`：32 个整数寄存器，两个组合读端口；`x0` 始终读为零且禁止写入。
- `FloatingRegisterFile.scala`：32 个浮点寄存器；`f0` 可写，RV64F 写回时执行 NaN-boxing。

读端口是组合逻辑，写端口仅在后端的 `commitFire` 条件下更新。这样寄存器可见性由流水线的
提交顺序定义，而不是依赖同一时钟周期的“前半拍读、后半拍写”约定。
